export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || "https://epsilon.proto.aalto.fi/api"

// Storage keys
export const STORAGE_KEYS = {
  AUTH_TOKEN: "authToken",
  CURRENT_USER: "currentUser",
}
